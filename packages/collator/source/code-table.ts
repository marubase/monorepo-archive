export const CodeTable = {
  AOEND: Buffer.from([0]),
  AAEND: Buffer.from([1]),
  ASEND: Buffer.from([2]),
  ABEND: Buffer.from([3]),
  ANULL: Buffer.from([4]),
  AFALSE: Buffer.from([5]),
  ATRUE: Buffer.from([6]),
  ANINT8: Buffer.from([7]),
  APINT8: Buffer.from([8]),
  AUINT8: Buffer.from([9]),
  ANINT16: Buffer.from([10]),
  APINT16: Buffer.from([11]),
  AUINT16: Buffer.from([12]),
  ANINT32: Buffer.from([13]),
  APINT32: Buffer.from([14]),
  AUINT32: Buffer.from([15]),
  ANINT64: Buffer.from([16]),
  APINT64: Buffer.from([17]),
  AUINT64: Buffer.from([18]),
  ANFLOAT32: Buffer.from([19]),
  APFLOAT32: Buffer.from([20]),
  ANFLOAT64: Buffer.from([21]),
  APFLOAT64: Buffer.from([22]),
  ANDATE: Buffer.from([23]),
  APDATE: Buffer.from([24]),
  AVERSION: Buffer.from([25]),
  ABSTART: Buffer.from([26]),
  ASSTART: Buffer.from([27]),
  AASTART: Buffer.from([28]),
  AOSTART: Buffer.from([29]),
  AUNDEF: Buffer.from([30]),
  AACOMMA: Buffer.from([124]),
  AOCOLON: Buffer.from([125]),
  AOCOMMA: Buffer.from([126]),
  AESCAPE: Buffer.from([127]),

  DOEND: Buffer.from([255 - 0]),
  DAEND: Buffer.from([255 - 1]),
  DSEND: Buffer.from([255 - 2]),
  DBEND: Buffer.from([255 - 3]),
  DNULL: Buffer.from([255 - 4]),
  DFALSE: Buffer.from([255 - 5]),
  DTRUE: Buffer.from([255 - 6]),
  DNINT8: Buffer.from([255 - 7]),
  DPINT8: Buffer.from([255 - 8]),
  DUINT8: Buffer.from([255 - 9]),
  DNINT16: Buffer.from([255 - 10]),
  DPINT16: Buffer.from([255 - 11]),
  DUINT16: Buffer.from([255 - 12]),
  DNINT32: Buffer.from([255 - 13]),
  DPINT32: Buffer.from([255 - 14]),
  DUINT32: Buffer.from([255 - 15]),
  DNINT64: Buffer.from([255 - 16]),
  DPINT64: Buffer.from([255 - 17]),
  DUINT64: Buffer.from([255 - 18]),
  DNFLOAT32: Buffer.from([255 - 19]),
  DPFLOAT32: Buffer.from([255 - 20]),
  DNFLOAT64: Buffer.from([255 - 21]),
  DPFLOAT64: Buffer.from([255 - 22]),
  DNDATE: Buffer.from([255 - 23]),
  DPDATE: Buffer.from([255 - 24]),
  DVERSION: Buffer.from([255 - 25]),
  DBSTART: Buffer.from([255 - 26]),
  DSSTART: Buffer.from([255 - 27]),
  DASTART: Buffer.from([255 - 28]),
  DOSTART: Buffer.from([255 - 29]),
  DUNDEF: Buffer.from([255 - 30]),
  DACOMMA: Buffer.from([255 - 124]),
  DOCOLON: Buffer.from([255 - 125]),
  DOCOMMA: Buffer.from([255 - 126]),
  DESCAPE: Buffer.from([255 - 127]),
};
